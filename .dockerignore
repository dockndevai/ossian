# Build outputs and dependencies are produced inside the image; copying them in makes the
# context large and can leak a host-built jar into a "clean" build.
**/target/
**/node_modules/
**/dist/
.git/
docs/media/
*.log
