<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.3</version>
    <relativePath/>
  </parent>

  <groupId>io.github.dockndevai</groupId>
  <artifactId>ossian-parent</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>ossian-parent</name>
  <description>Open-book RAG over your own documents: Spring Boot service, React console, Keycloak, pgvector</description>

  <modules>
    <module>backend</module>
  </modules>

  <properties>
    <java.version>21</java.version>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- Spring AI 1.0.0 GA. Note the artifact naming changed at 1.0.0: it is
         spring-ai-starter-*, not the pre-M7 spring-ai-*-spring-boot-starter. -->
    <spring-ai.version>1.0.0</spring-ai.version>
    <springdoc.version>2.8.6</springdoc.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.ai</groupId>
        <artifactId>spring-ai-bom</artifactId>
        <version>${spring-ai.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <!-- JDK 23+ ignores classpath annotation processors unless asked. -->
          <configuration><proc>full</proc></configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
